CREATE TABLE  "ORDEER" 
   (	"ORDER_ID" NUMBER(10,0), 
	"USER_ID" NUMBER(10,0), 
	"PRODUCT_ID" NUMBER(3,0), 
	"QUANTITY" NUMBER(4,0), 
	"PRICE" NUMBER(5,0), 
	"ORDER_DATE" DATE, 
	 CONSTRAINT "PK_ORDER" PRIMARY KEY ("ORDER_ID") ENABLE
   )
/
ALTER TABLE  "ORDEER" ADD CONSTRAINT "ORDER_USER_FK" FOREIGN KEY ("USER_ID")
	  REFERENCES  "EUSER" ("USER_ID") ENABLE
