

CREATE TABLE  "SHOP" 
   (	"SHOP_ID" NUMBER(5,0), 
	"SHOP_REQUEST_ID" NUMBER(5,0), 
	 PRIMARY KEY ("SHOP_ID") ENABLE
   )
/
ALTER TABLE  "SHOP" ADD FOREIGN KEY ("SHOP_REQUEST_ID")
	  REFERENCES  "SHOP_REQUEST" ("SHOP_REQUEST_ID") ENABLE
/

CREATE OR REPLACE TRIGGER  "SHOP_TRIGGER" 
BEFORE INSERT ON SHOP 
FOR EACH ROW 
BEGIN 
    IF :NEW.shop_id IS NULL THEN 
        :NEW.shop_id := shop_seq.NEXTVAL; 
    END IF; 
END; 

/
ALTER TRIGGER  "SHOP_TRIGGER" ENABLE
/